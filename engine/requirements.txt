lancedb>=0.6.0
sentence-transformers>=2.7.0
watchdog>=4.0.0
pyarrow>=15.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0
rank-bm25>=0.2.2
pyyaml>=6.0

# Optional — recommended
psutil>=5.9.0              # CPU gating: skip reranker when CPU is saturated

# Optional — only needed for specific embedding models
# einops>=0.7.0            # Required by nomic-embed-text-v1.5
